import "./globals.css";

export const metadata = {
  title: "Patrick's Notre Dame Linebacker Workout",
  description: "Workout Tracker",
  icons: {
    icon: "/logo.png",
    apple: "/logo.png",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}